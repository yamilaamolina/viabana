@using ProyectoFinal.Models;

@model ModuloCompras
@{
    ViewBag.Title = "Compra";
    //ViewBag.Proveedores = Model.ListaProveedores as IEnumerable<SelectListItem>;

}

<dl class="menulateral">
    @{ Html.RenderAction("Modulos", "Home"); }
</dl>

<div class="col-lg-12 informativo">

    @*<div>
        <button type="button" id="Nueva Compra" data-toggle="modal" data-target="#newCompra">
            Agregar Nueva Compra
        </button>
    </div>*@

    <div>
        <label>
            Proveedor
        </label>
        <select>
            @foreach (var proveedor in Model.ListaProveedores)
            {
               <option>@proveedor.RazonSocial</option>
            }
        </select>
        <button class="btn btn-danger">Ver productos</button>      
    </div>
    @*<table  class="table table-hover table-condensed table-responsive container" style="top: 25px; position:absolute">
        <tr>
            <td><strong>ID Compra</strong></td>
            <td><strong>Proveedor</strong></td>
            <td><strong>Monto</strong></td>
            <td><strong>Fecha</strong></td>
            <td></td>
            <td></td>
        </tr>
        @foreach (var compra in Model.ListaDeCompra)
        {
            <tr>
                <td id="idcompra">@compra.IdCompra</td>
                <td>@compra.Proveedor.RazonSocial</td>
                <td>@compra.TotalCompra</td>
                <td>@compra.FechaCompra.Date</td>
                <td><span id="ok" class="glyphicon glyphicon-pencil delbtn" data-toggle="modal" data-target="#editProv"></span></td>
                <td><span class="glyphicon glyphicon-trash" data-toggle="modal" data-target="#deleteProv"></span></td>
            </tr>
        }
    </table>*@
</div>
